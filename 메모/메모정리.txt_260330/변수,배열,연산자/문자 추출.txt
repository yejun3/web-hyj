**문자 추출**

- charAt()메서드는 **매개값으로 주어진 인덱스의 문자를 반환한다.**
- **인덱스** :   0에서부터 순차적으로 증가하는 숫자를 말한다.

```java
String subject = "자바 프로그래밍";
char charValue = subject.charAt(3); 
// subject변수에 들어있는 문자열의 3번 index에 해당하는 문자를 반환
```