출력문
System.out.printf( ＼n"); -> 줄 바꿈
//printf();
문자열 속에서 데이터를 출력할 수 있는 메서드
데이터는 자료형에 따라 서식문자를 이용해 출력한다.
정수 : %d  //실수 : %f
문자형 : %c //문자열 : %s
//논리형 : %b

Scanner sc = new Scanner (System.in);
// 값을 유동적으로 쓰고 싶을 떄 사용
// 스캐너 기능을 가져와서 사용한다. 그리고 사용할 때 이름은 sc로 지정한다. 라는 뜻
// import를  설정 해줘야한다.
// import 단축키: ctrl키 + shift키 + o 
// : 다른 파일에서 작성된 것을 가져오기 위해 import을 한다.
-> 설정시 import java. utill. Scanner가 위에 생김
 
int a = sc. nextInt();
String address = sc.nextLine();

//난수생성
int i = (int)(Math.random()*45+1);

스캐너 사용방법
(1) 스캐너를 사용하기 위한 코드를 사용
Scanner sc = new Scanner (System.in);
(2) import를한다 //  import 단축키: ctrl키 + shift키 + o
-> 설정시 import java. utill. Scanner가 위에 생김

(실행이 되는 해당 출력문을 적어서 의도를  
int battery = sc. nextInt(); // battery 라는 공간을 만듦
// sc. nextInt(); : 정수형 데이터를 받을 수 있는 기능을 만듦
String address = sc.nextLine(); // 변수 공간 만듦
// sc. nextLine() : 문자형 데이터를 받을 수 있음 

// 절전 모드 여부 확인
System.out.print("절전여부 상태를 입력하세요(true/false)")
boolean saveMode= sc.nextboolean();