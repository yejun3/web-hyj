0402 메모
 상속
- 관련있는 것끼리 묶은것
공통적인 속성(필드, 맴버)이나 동작(메서드)를 부모 쪽에 몰아넣는것
자식은 부모클래스를 상속받아서 사용
n상속 가능 // 상속 -> 상속 -> 상속
다중 상속은 불가능함 상속받는 부모클래스는 1개만 가능

Object : 모든 클래스의 부모클래스

Arrays.toString(배열 변수)//배열을 문자열로 출력

sealed : 봉인된 클래스 
permit 내가 이용한 클래스만 상속할 수 있음

인터페이스
일반 필드 x
상수,추상적 메소드 ,디폴트 메서드, 정적 메서드, private메서드 가능

ㄴ동작에 관한 가이드라인 제공
같은 범주에 있는 것들이 아니어도 됨

추상클래스와 마찬가지로 직접객체를 만드는 것이 불가능하다


인터페이스를 구현할 클래스가 필요함
ㄴ> class Impl implements 인터페이스명
추상 메서드를 반드시 오버라이딩 해야한다.

다중 구현이 가능하다.
각 인터페이스에 있는 추상메서드를 전부 오버라이딩 해야한다.

다중상속이 가능하다.
-상속은 인터페이스 끼리만 가능하다.
interface interA extends InterB InterC


중첩클래스
- 많이 사용되지는 않지만 특정한 상황에서 쓰인다.

1.클래스 내부에서만 쓰는 전용 객체
(외부에 공개할 필요가 없음)

정적내부 클래스 
내부클래스이데 static이 붙은 형태이다.
바깥객체에 소속된 내부클래스처럼 보이지만 
실제로는 바깥 객체 없이도 독립적으로 

