0407복습
자바에서 제공하는 클래스
Object클래스 :모든 클래스의 최상위 클래스 다른 클래스는 상속받아서 오버라이딩 해서 쓰는 경우가 많음
toString(), equals()

System클래스 : 출력 입력과 관련된 클래스
in, out, err

exit() :프로그램을 종료시키는 메서드

시간을 측정하는 메서드
nanoTime()
currentTimeMillis() 

운영체제의 속성읽기
운영체제 이름, 사용자명 , 사용자 폴더 , 자바버전...

String 
수정되지 않는다는 규칙때문에 자주 변경되면 메모리에 버려진 문자들이 많아짐

Stringbuilder
버퍼(buffer): 데이터를 한 번에 처리하지 않고 임시로 저장하는 공간
모아서 한번에 처리한다.
입출력하는것은 메모리를 많이 사용하기 때문레 모았다가 한번에 처리하는게 속도가 더 빠르다.

수정이 일어나면 기존에 객체를 수정한다.
appen(): 맨뒤에 추가
insert(): 내가 원하는 위치에 추가
delete(): 내가 원하는 만큼 삭제
reverse(): 문자열 뒤집기

StringTokinzer
구분자가 1개라면 문자열을 잘라서 가지고 있는 것
hashMoreTokens(): 남아있는 문자열이 있는지 여부
nextToken(): 문자열을 하나씩 가져온다.

포장클래스(Wrapper)
기본타입의 클래스
integer: 정수형  ex) Integer number = 100; 객체 취급한다.(100의 값을 가지는 객체)
Charater:문자형
나머지는 앞이 대문자

Math 클래스 
필드, 메서드가 거의 static으로 구성되어 있기 때문에 따로 객체를 생성을 안해도 된다.
절대값: int v1 = Math.abs(-5);
올림 :double v3 = Math.ceil(5.3);
내림: double v5 = Math.floor(5.3);
반올림: long v12 = Math.round(5.3);
최대값 : int v7 = Math.max(5,9);
최소값:  int v8 = Math.min(5,9);
랜덤값:   double v11 = Math.random();

LocalDateTime
날짜와 시간을 읽고 조작하기 위해 사용
시간을 관리하는 자료형
객체를 직접 만들지 않음
LocalDateTime.now(): 현재 날짜와 시간을 반환
of(년,월,일,시,분): 특정날짜 생성

getYear()
getMonth
getDatOfMonth
getHour
get

withYear()
withHour() 날짜와 식간을 수정하는 것이 가능하다.
 
isBefore()
isAfter()
isEqual()

Local Date : 날짜만
LocalTime : 시간만

정규표현식(Regular Expression)
믄자열의 "패턴"을 정의하는 규칙
특정 문자열이 내가 원하는 형식으로 적혀있는지 검사
a-z : 소문자
A-Z : 대문자
0-9: 0부터 9까지
가-힣
? :djqtrjsk gksro
+:한개거나 이상
[]: 안에 적힌 것 중 한개 ex)[abc]-> a,b,c중에 한글자
문자열.matches(정규식)
Pattern.matches(regExp, ssn);