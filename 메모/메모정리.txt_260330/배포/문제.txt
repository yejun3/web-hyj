문제
자바
- 객체를 만들 떄 new해서 객체를 메모리에 올리기 위한 공간 만듦
- GC가 사용되지 않는 객체를 자동으로 메모리에서 제거 
- 메모리 오류 위험이 적다.
C
- 메모리를 얼마 공간 만큼 할당해달라고 직접 요청 (함수)
malloc(sizeof(int)*4)
- free() 함수를 이용해서 메모리에서 해제
- 상대적으로 크다.
- 메모리 최적화에 좋다.
- 언어 자체가 가볍다.

메모리 누수 : 메모리를 점유했는데 해제가 안된 것

C-> 클래스/ 객체, 상속, 오버로딩, 예외처리 개념이 없음
C++ ->객체 지향을 지원 new를 사용
C# -> C와 C++의 문법적 영향을 받았지만 , 실행 방식과 메모리 관리 측면에서는 Java와 상당히 비슷하다.


문제 1.

한 개발팀에서 다음과 같은 작업을 수행하였다.

작업 A
Java 소스코드 작성
↓
컴파일
↓
테스트 수행
↓
라이브러리 결합
↓
application.jar 생성
작업 B
application.jar
↓
테스트 서버로 전송
↓
서버 환경 설정
↓
application.jar 실행
↓
서비스 정상 동작 확인

신입 개발자는 다음과 같이 말했다.

“A와 B는 모두 프로그램을 실행하기 위한 과정이므로 사실 같은 작업이라고 생각합니다.”

문제
작업 A와 작업 B가 각각 어떤 작업인지 작성하시오.
- A : 빌드 - 소스코드를 컴파일하고 패키징하여 jar,war등의 실행 가능한 상태의 결과물을 만드는 과정
- B : 배포 - 빌드된 결과물과 실행에 필요한 리소스를 실제 서버 환경으로 이동하여 설정하고 실행하는 과정

두 작업의 목적과 결과물의 차이를 설명하시오

작업 A가 정상적으로 완료되었더라도 사용자가 바로 서비스를 사용할 수 없는 이유를 설명하시오.
빌드가 완료되어 Application.jar 가 만들어졌더라도 아직 실제 서버에 적용되지 않았다면 사용자는 해당 서비스를 이용할 수 없다.


문제 2.

세 프로젝트의 개발 환경이 다음과 같다.

프로젝트   개발 언어   현재 상태
A   C++   .cpp 소스 존재
B   Java   .java 소스 존재
C   Python   .py 소스 존재

개발팀에서는 세 프로젝트에 완전히 동일한 빌드 절차를 적용하려고 한다.

문제
A~C 프로그램이 실행되기까지의 처리 방식을 각각 설명하시오.
- A : C++은 소스코드를 컴파일 하여 기계어 형태로 변환하고 생성된 실행 프로그램을 실행한다.
- B : Java는 .java파일을 컴파일하여 중간 코드인 class를 생성한 후 같은 실행 환경에서 실행한다.
- C : Python은 프로그램 전체를 먼저 기계어 실행 파일로 만드는 방식이 아니라 인터프리터가 코드를 해석함면서 실행하는 방식이다.
Java의 컴파일 결과가 C++과 동일한 형태의 최종 기계어 실행 파일이 아닌 이유를 설명하시오.

세 프로젝트에 동일한 빌드 절차를 적용하는 것이 적절한지 판단하고 이유를 작성하시오.
- 세 언어는 변환 및 실행 방식이 서로 다르므로 동일한 빌드 절차를 그대로 적용하는 것은 적절하지 않다.


문제 3.

C 언어 프로젝트에서 다음 문제가 발생하였다.

상황 A
#include <stdio.h>

와 같은 내용을 컴파일 전에 처리해야 한다.

상황 B

다음 코드에서 오류가 발견되었다.

int number =
상황 C

여러 소스 파일을 각각 변환한 결과가 존재한다.

main
calculator

최종적으로 하나의 실행 프로그램으로 만들어야 한다.

문제
A~C에서 수행되는 빌드 과정의 활동을 각각 작성하시오.
- A: 전처리
- B: 파싱
- C: 링킹

- 전처리는 실제 컴파일전에 헤더 파일 등의 필요한 내용을 처리한다.

상황 B의 오류가 빌드 과정에서 발견되는 이유를 설명하시오.
- 파싱은 작성한 코드가 해당 프로그래밍 언어의 문법에 맞는지 분석한다. 따라서 int number = 처럼 문법적으로 완성되지 않은 코드는 이 과정에서 문제가 발견될 수 있다.

상황 C에서 해당 과정이 필요한 이유를 설명하시오.
링킹은 여러 목적 파일 및 라이브러리를 연결하여 최종 파일을 생성하는 과정이다.

문제 4.

회사 웹 서비스에서 다음 요청이 발생하였다.

요청 A
/images/company-logo.png
요청 B
/index.html
요청 C
POST /equipment/register


설비 정보 확인
↓
DB 저장
↓
등록 결과 반환
문제
A~C를 Web Server와 WAS 중 어느 쪽에서 주로 처리하는 것이 적절한지 각각 판단하시오.
- A,B는 HTML 이미지와 같은 정적 리소스이므로 Web Server에서 처리하는 것이 적절하다.
- C는 프로그램 로직을 실행하고 DB를 처리해야 하는 동적 요청이므로 WAS가 담당하는 것이 적절하다.

Web Server와 WAS의 역할 차이를 설명하시오.
- Web Server는 HTML , 이미지 등 정적 콘텐츠의 제공을 주로 담당하며 ,WAS는 비즈니스 로직과 같은 동적 프로그램 처리를 담당한다.
다음 구조에서 각 구성요소가 담당하는 역할을 설명하시오.

사용자 
↓
Web Server
↓
WAS
↓
DB


문제 5.

다음 세 애플리케이션을 배포하려고 한다.

프로젝트 A
Java 기반
Spring Boot 사용
독립적으로 실행
하나의 서비스 형태
프로젝트 B
JSP
Servlet
Java Class
기존 WAS에 웹 애플리케이션으로 배포
프로젝트 C
여러 엔터프라이즈 애플리케이션 모듈을 하나의 단위로 묶어 배포
문제
A~C에서 적절하게 사용할 수 있는 배포 패키지 형식을 각각 작성하시오.
- A : Jar -java클래스와 라이브러리 리소스 등을 묶는데 사용할 수 있으며 SpringBoot와 같이 독립적으로 실행되는 어플리케이션에서 사용할 수 있다.
- B : WAR - JSP, Servlet, Java class 등으로 구성된 웹 어플리케이션을 WAS에 배포할 수 있다.
- C : EAR -여러 엔터프라이즈 어플리케이션 모듈을 하나로 묶는 형식
각각 그렇게 판단한 이유를 설명하시오.
“Java 프로젝트는 모두 같은 파일 형식으로 배포하면 된다.”라는 주장에 대해 적절한지 판단하고 이유를 작성하시오.
- 동일한 java 계열 기술이라도 프로젝트 구조와 배포 환경에 따라 적절한 배포 형태가 달라진다.


문제 6.

신규 업무시스템을 운영환경에 배포하려고 한다.

배포 대상은 다음과 같다.

company-logo.png
main.html
inventory.war
NightBatch.class
interface.xml
common-library.jar
framework-config.xml

한 개발자는 다음과 같이 제안하였다.

“배포 파일이니까 전부 같은 디렉터리에 복사하면 관리하기 편할 것 같습니다.”

문제
각 파일의 리소스 유형을 판단하고 적절한 배포 방법을 작성하시오.
- company-logo.png ->이미지 -> 웹서버
main.html -> HTML -> 웹서버
inventory.war -> 웹단 프로그램 -> WAR 형태로 배포
NightBatch.class -> 배치 프로그램 -> 컴파일된 class파일 배포
interface.xml -> 인터페이스 전문 -> xml 형태 배포
framework-cinfig.xml -> 환경 , 프레임워크 설정 관련 리소스 -> 정의된 방식에 따라 배포
- 모든 파일을 동일한 방법으로 배포하는 것이 적절하지 않은 이유를 설명하시오.
배포 대상은 파일 유형 마다 실행되는 위치와 처리 방법이 다르므로 동일한 위치와 방식으로 복사하는 것은 적절하지 않다.
- 실제 배포 전에 배포 정의서 등을 통해 확인해야 할 내용을 3가지 이상 작성하시오.


문제 7.

프로젝트에서는 다음과 같은 개발 절차를 사용하고 있다.

개발자 코드 수정
↓
운영 서버에 직접 파일 복사
↓
프로그램 실행
↓
문제가 발생하면 개발자 PC에서 테스트
↓
마지막에 형상관리 시스템에 소스 등록

프로젝트 관리자는 다음 목표를 가지고 있다.

개발자가 새로운 소스코드를 등록할 때마다 자동으로 빌드와 테스트를 수행하여 문제를 빠르게 확인한다.

문제
현재 절차에서 문제가 되는 부분을 3가지 이상 작성하시오.
- 소스가 형상관리 시스템에 등록되기 전에 운영 서버에 직접 배포함
- 테스트 전에 운영 환경에 반영함
- 개발자 Pc와 운영 환경 간 결과가 달라질수 있음
- 여러 개발자의 변경사항을 일관되게 관리하기 어려움
프로젝트 관리자의 목표에 맞는 개선된 처리 흐름을 순서대로 작성하시오.
- 개발자 코드 변경 -> 형상관리 시스템 반영 -> 자동빌드 -> 자동 테스트 -> 문제 확인 
개선된 환경을 적용했을 때 기대되는 효과를 설명하시오.
- CI를 적용하면 코드 변경 시 반복적으로 통합 빌드 테스트를 자동으로 수행하여 문제를 빠르게 발견할 수 있다.


문제 8.

회사의 애플리케이션 운영환경은 다음과 같다.

개발 서버
테스트 서버
운영 서버
DR 서버

한 신입 개발자가 다음과 같이 말했다.

“운영 서버가 가장 성능이 좋으니까 개발 중인 기능도 운영 서버에서 직접 개발하고 확인하는 게 가장 정확하지 않나요?”

문제
신입 개발자의 제안이 적절한지 판단하고 이유를 작성하시오.
- 신입 개발자의 제안은 적절하지 않다.

개발 서버, 테스트 서버, 운영 서버, DR 서버의 목적을 각각 설명하시오.
- 개발 서버 : 개발 및 기능 확인 
- 테스트 서버 : 통합 및 품질 검증
- 운영 서버 : 실제 사용자에게 서비스 제공
- DR 서버 : 장애나 재해 발생 시 서비스 복구

운영 서버에서 개발 작업을 직접 수행할 경우 예상할 수 있는 문제를 작성하시오.
- 운영 서버에서 개발을 직접 수행하면 실제 사용자의 서비스에 영향을 줄 수 있고 검증되지 않은 코드가 운영환경에 반영될 수 있다.

DR 서버가 사용될 수 있는 상황을 설명하시오.
- 장애 발생이나 재해 발생시 서비스를 복구해야하는 상황에서 사용

문제 9.

운영 시스템이 다음과 같이 구성되어 있다.

             ┌→ WAS 1
Load Balancer├→ WAS 2
             └→ WAS 3

새로운 v3.0을 배포하면서 작업자는 WAS 1에만 파일을 적용하였다.

배포 후 사용자에게 다음 현상이 나타났다.

같은 기능을 실행했는데 어떤 경우에는 새로운 화면이 나오고 어떤 경우에는 이전 화면이 나온다.

문제
이러한 현상이 발생한 원인을 설명하시오.
- WAS 1만 v3.0으로 변경하고 WAS 2와 WAS 3는 이전 버전이므로 로드밸런서가 요청을 어는 서버로 전달하는지에 따라 서로 다른 프로그램 결과가 나타날 수 있다.
올바른 배포 방법을 작성하시오.
- 따라서 여러 서버가 동일한 서비스를 제공한다면 필요한 서버에 동일한 어플리케이션 버전을 배포해야한다.
여러 서버에 분산 배포할 때 반드시 확인해야 하는 사항을 서술하시오.
- 배포 후에는 서버별 배포 버전과 정상 동작 여부를 확인해야한다.

문제 10.

다음 프로젝트의 빌드·배포 환경을 새로 구성해야 한다.

프로젝트 조건
개발 언어 : Java 21
프레임워크 : Spring Boot
개발 인원 : 10명


모든 소스는 형상관리 시스템에서 관리


개발자가 코드를 변경하면
자동으로 빌드 수행


빌드 완료 후
자동 테스트 수행


테스트가 성공하면
테스트 서버에 배포


운영 환경은 서버 3대로 구성


장애 및 재해 발생에 대비하여
별도의 DR 서버 운영
문제
개발자가 코드를 변경한 시점부터 테스트 서버에 배포되기까지의 전체 흐름을 작성하시오.
- 개발자 코드 변경 -> 형상 관리 시스템 반영 -> 최신 소스 가져오기 -> 자동 빌드 -> 자동 테스트 -> 배포 파일 생성 -> 테스트 서버 배포 -> 정상 동작 확인 -> 운영 서버 배포 
각 단계에서 필요한 도구의 역할을 다음 기준으로 구분하여 설명하시오.
형상관리 -> 소스 및 변경 이력 관리
빌드 -> 컴파일 테스트 연계 패키징 및 실행 결과물 생성
테스트 -> 프로그램의 정상 여부 검증
배포 -> 배포 도구 -> 빌드 결과물을 서버에 적용
운영 서버가 3대로 구성된 환경에서 신규 버전을 배포할 때 주의해야 할 사항을 작성하시오.
- 운영 서버 3대는 동일한 서비스를 처리하므로 동일한 버전을 일관되게 배포해야한다.
개발 서버, 테스트 서버, 운영 서버, DR 서버가 각각 필요한 이유를 설명하시오.
- 각 서버는 개발, 테스트, 실제 서비스, 장애 재해 복구라는 점에서 서로 다른 목적을 가진다. 
위 프로젝트에 적합한 전체 빌드·배포 구성도를 글 또는 화살표 형태로 작성하시오.