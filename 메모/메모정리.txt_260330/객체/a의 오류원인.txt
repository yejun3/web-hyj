a의 오류원인
: ArrayIndexOutOfBoundsException 에 해당하는 오류입니다.
scores의 배열길이는 4지만 배열의 인덱스는 0,1,2,3 까지이기때문에 배열 범위 밖에 접근하게 된다.
이대로 반복문을 실행할 경우 i는 scores[4]라는 배열 범위 밖에 접근하게된다.

B의 경우 average의 타입이 정수형 int이기 때문에 소수점 이하의 값이 버려진다.
따라서 정확한 평균값을 구하려면 타입을 double로 바꾸고 나눗셈 연산도 실수형이 나오도록 해야한다.


==연산자는 문자열이 같은지 비교하는 것이 아니라 참조주소값을 비교한다.
동등성을 비교하는것이 아니라 동일성을 확인한다.
문자열을 비교하기 위해서는 equals()를 써야한다.

price*0.8의 연산 결과는 실수인데 discountedPrice는 정수형 타입인 이므로 타입 불일치가 일어난다.

(int)(price*0.8)로 형변환을 해주어야 한다.



