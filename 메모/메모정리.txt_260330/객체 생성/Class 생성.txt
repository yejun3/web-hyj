Class 구성
클래스를 구성하는 요소는 필드,메서드, 생성자 3가지

필드(field)
- 각 객체가 가져야할 데이터의 상태를 저장하는 변수
인스턴스 변수 지정
// 필드지정
// 객체마다 가지는 정보
ex )
class Person { 
String name; // 
int age;
Static int personCount =0; // 클래스변수 모든 객체가 공유하는 변수
}                                    

//1. 선언과 동시에 값을 초기화한다.
	String nation = "대한민국";
//객체마다 동일한 값을 갖고 있다면 필드 선언 시 초기값을 대입
	
//2. 생성자를 호출하면서 매개변수로 값을 넘겨준다.
//객체마다 다른 값을 가진다면 생성자에서 필드를 초기화 하는 것이 좋다.
// 가능하면 초기화시킬 필드명과 동일한 이름을 사용하는 것이 좋다.

	final String ssn;
         String name;
	
	//생성자 생성
	public Korean(String ssn,String name) {
	this.ssn= ssn;
	this.name= name;
	}

this 
class Person 

This 
(인스턴스 변수와 지역변수(전달값 매개변수)를 구분하기 위해 사용)
// 자기자신을 의미
this. 인스턴스변수
인스턴스변수와 전달값은 받은 변수를 구분짓기 위해 사용
this.name = name
this.name(인스턴스 변수를 의미) = name(전달값을 의미)

return
매개변수,매서드 내부에서 정의된 변수는 지역변수이다.
메서드가 호출되서 실행한다음 종료가 되면 메모리에서 삭제된다.
만약, 메서드 밖에서 연산결과를 사용하고 싶다면 return 값을 통해 
값을 밖으로 꺼내야 한다.

매서드명 메서드를 호출할 때 사용한는 이름
-첫글자는 소문자로 사용

