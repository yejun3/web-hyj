0403복습

중첩클래스
-인스턴스 내부클래스
-정적내부클래스(static): buiider패턴
-지역내부클래스: 외부클래스의 메서드 내부에서 만들어지는 클래스

특징 
외부의 노출되지않는다.

익명 클래스
이름이 없는 클래스 
인터페이스와 같이 사용되는 경우가 많다
인터페이스는 직접 객체를 생성할 수 없기 때문이다.

interface InterA{}
class Impl implemnts InterA{...}

class Main{
main(){
Impl = new Impl(); 인터페이스를 구현한 객체

InterA inter = new InterA(){
// 클래스명이 없는 형태
//이 공간이 클래스 Impl과 같은 역할
}

다형성
클래스 타입변환
상속
자식타입비-> 부모타입

인터페이스
구현클래스타입 -> 인터페이스타입

생각해야하는점
- 매서드를 정의하면서 매개변수를 받아야하나?
- 어떤 기능을 정의해야하는지? 생각할 것
- 정의할 때는 매개변수에 값이 들어있는게 아님
- 매개변수에 어떤값이 들어올지 예상하고 안에 기능을 만들어야함
- 메서드 구조
접근제한자 반환형타입 매서드명(매개변수){}
return 반환값이 있으면 연산이끝나고 값을 매서드 밖으로 꺼냄
반환값의 타입은 반환형이 같아야한다.
new Student()의 타입은 Student

라이브러리, 모듈
-클래스, 인터페이스의 모음
-JAR 파일로 존재한다.
-자주 재사용하는 클래스, 인터페이스, 메서드 같은 것들을 추출해서 관리한다.
//라이브는 내가 추출한 시점에서 저장
//모듈은 패키지 관리 기능까지 포함된다.

- JAR파일을 등록하거나 , 프로젝트 자체를 참조하는 방법

