**문제1**

- 다음 PL/SQL 코드를 실행했지만 화면에 아무 메시지도 출력되지 않았다.
    
    ```sql
    BEGIN
        DBMS_OUTPUT.PUT_LINE('테스트 성공');
    END;
    /
    ```
출력 결과를 확인하기 위해 실행 전에 입력해야 하는 SQL*Plus 명령어를 작성하시오.
: SET SERVEROUTPUT - (출력한 내용을 화면에 표시하도록 설정)

---

문제2
- 다음은 Oracle SQL*Plus에서 프로시저를 생성한 결과이다.
    
    ```
    Warning: Procedure created with compilation errors.
    ```
 
1. 이 메시지의 의미를 설명하시오.
2. 오류의 원인을 확인하기 위해 사용해야 하는 `SQL*Plus` 명령어를 작성하시오.
: 프로시저는 만들었지만 컴파일 오류가 있음
  SHOW ERRORS를 사용해서 오류 확인

---

**문제3**

- 다음은 급여를 수정하는 프로시저이다.
    
    ```sql
    CREATE OR REPLACE PROCEDURE update_sal
    IS
    BEGIN
    	UPDATE emp
    	SET sal= sal+100;
    END;
    /
    ```
    
- 프로시저가 정상적으로 동작하는지 테스트하기 위한 절차를 순서대로 작성하시오.
: 프로시저 테스트
 1. 실행전 데이터 확인
SELECT empno, ename, sal
FROM emp;

 2. 프로시저 실행
EXECUTE update_sal
 
3. 실행 후 데이터 확인
SELECT empno, ename, sal
FROM emp;

 4. 의도대로 작동되지 않으면 디버깅

---

**문제4**

- 다음 SQL*Plus 명령어의 용도를 작성하시오.
    
    ```sql
    SPOOL result.txt
    ```

 : result.txt에 SQL 실행 결과를 저장함

---

**문제5**

다음 코드를 실행하였다.

```sql
VARIABLE v_count NUMBER;

EXEC :v_count :=50;

PRINT v_count;
```

1. VARIABLE 명령어의 역할을 설명하시오.
2. PRINT 명령어의 역할을 설명하시오.

: 1. 바인드 변수를 생성하는 명령어
  2. 바인드 변수에 저장된 값을 출력하는 명령어
---

**문제6**

다음 저장 객체들의 차이점을 설명하시오.

- Stored Function
- Stored Procedure

: Stored Function은 데이터베이스에 저장해두고 필요할 떄 호출하는 함수로
   반환값이 있다. -> return을 사용한다.
  Stored Procedure는 데이터베이스에 저장해두고 필요할 떄 실행하는 함수로 
  데이터 처리작업에 초점을 두고 있다. 반환값이 필수는 아니다.
---

**문제7**

다음 조건을 만족하는 패키지 실행 코드를 작성하시오.

조건

<aside>

- DBMS_OUTPUT 출력 결과가 보이도록 설정
- 패키지명 : EMP_INFO
- 프로시저명 : ALL_EMP_INFO
</aside>

: SET SERVEROUTPUT ON
   EXEC EMP_INFO.ALL_EMP_INFO;

---

**문제8**

- 개발자가 다음 명령을 실행하였다.
    
    ```sql
    SPOOL result.txt
    
    SELECT *
    FROM emp;
    ```
    
- 그런데 result.txt 파일이 생성되었지만 내용이 계속 추가되고 있었다.
- 이러한 현상이 발생한 이유를 설명하고 해결 방법을 작성하시오.

: SPOOL 기능이 종료되지 않았기 떄문이다. 그러므로 SPOOL을 사용하고
   SPOOL OFF 를 작성해서 닫아야함
---

**문제9**

- 개발팀은 다음 기능을 구현하려고 한다.
    
    ```
    회원이 삭제되면 삭제 시간과 회원번호를 자동으로 기록한다.
    ```
    
- 가장 적절한 저장 객체를 선택하고 이유를 설명하시오.

: TRRIGER  
-> 특정 상황이 발생하면 자동으로 실행되는 데이터 베이스 객체이므로
       삭제 상황시 삭제시간과 회원번호를 자동으로 기록할 수 있다.              
---

**문제10**

- 다음 프로시저는 사원 급여를 수정한다.
    
    ```
    update_sal(empno)
    ```
    
- 현재 학생이 작성한 테스트 케이스는 다음과 같다.
    
    ```
    사원번호 7369 입력
    → 정상 실행 확인
    ```
    
- 위 테스트 케이스가 부족한 이유를 설명하고 추가로 필요한 테스트 케이스를 2개 이상 작성하시오.
  : 정상 입력만 테스트하고 예외상황에 대한 검증이 없다.
    테스트 케이스 : 
존재하지 않는 사원번호, 
NULL 입력 ,음수 
사원번호 입력, 문자열 입력
-------------------------------------------------------------------------------------------------------------------------------------------------------------------

[문제 1] 
SQL 구문 분석(Parsing) 단계에서 옵티마이저가 생성한 실행 계획을 저장하는 Oracle의 내부 테이블 이름은 무엇인지 기술하시오.
: Plan Table

[문제 2] 
어플리케이션 성능 모니터링(APM) 도구 중, 단순히 서버의 CPU나 메모리 리소스를 감시하는 것을 넘어 사용자의 요청부터 DB 조회, 외부 API 호출까지의 전체 거래 과정을 추적하는 방식을 일컫는 용어를 기술하시오.
: 엔드 투 엔드 모니터링 도구

[문제 3] 
다음은 공유 풀(Shared Pool)과 데이터베이스 버퍼 캐시(Data Buffer Cache)에 대한 설명입니다. 대량의 회원을 조회하는 동일한 SQL문이 반복해서 실행될 때, 두 메모리 영역이 SQL 처리 흐름(구문 분석, 실행, 추출) 단계에서 각각 성능 최적화에 어떻게 기여하는지 메커니즘을 비교하여 서술하시오.

: 공유풀
- 이전에 실행된 SQL문과 실행 계획을 임시  보관하므로 동일한 SQL이 들어왔을 떄 
문법 검사 오브젝트 확인 실행계획 수립 등의 작업을 생략한다.

: 버퍼캐시
- 디스크에서 한 번 읽어온 데이터를 메모리에 유지하므로 동일한 데이터 조회 시
느린 디스크파일 I/O를 발생시키지 ㅇ낳고 메모리에서 바로 데이터를 읽어와 수행 속도를 극적으로 향상시킨다.

[문제 4] 
APM 리소스 모니터링 화면에서는 CPU 20%, 메모리 40%로 매우 정상적인 수치를 보이고 있으나, 실제 사용자는 "주문 완료 화면이 넘어가는 데 8초 이상 걸린다"며 불편을 겪고 있습니다. 이 현상이 발생할 수 있는 원인을 엔드투엔드(E2E) 모니터링 관점에서 분석하고, 성능 개선을 위해 개발자가 확인해야 할 내부 유틸리티 2가지를 제시하시오.
: 원인분석
-  리소스는 여유가 있으나 주문 처리 트랜잭션 내부에서  병목이 발생했다.
E2E  관점에서 볼 때 외부 결제 API 호출 응답 지연, 문자 발송 서버의 지연 혹은 특정 SQL의 락(Lock)이나 ㅂ;효율적인 실행 계획으로 인해 





내부 프로세스가 대기상태에 빠져 있을 가능성이 높다.

확인해야 할 유틸리티
-sql 내부의 자원 소모량과 실행시간을 상세히 분석하는 TKPROK와 해당 SQL이 인덱스를 타지 못하고 전체 테이블 스캔을 하는지 
 내부 길찾기 경로를 확인하는 EXPLAIN PLAN 을 활용해야한다.
