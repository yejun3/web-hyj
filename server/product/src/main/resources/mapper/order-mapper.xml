<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.korea.product.dao.OrderDAO">

	<insert id="insert">
		insert into orders (product_id, product_count)
		values(#{productId},#{productCount})
	</insert>
	
	<select id="findAll" resultType="OrderVO">
		select 
			o.order_id,
			o.product_id,
			p.product_name,
			p.product_price,
			o.product_count,
			p.product_price * o.product_count AS total_price,
			o.order_date
		from orders o
		inner join product p
		on o.product_id = p.product_id
		order by o.order_id desc
	</select>
</mapper>