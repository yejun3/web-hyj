1. 논리데이터 저장소 확인하기
ㄴ 논리 데이터 모델링, 검증
모델링 절차 : 개념 -> 논리 -> 물리
엔티티
속성
관계

함수적 종속성 : A 가 B를 결정하는 것
- 완전함수 종속
- 부분함수 종속
- 이행함수 종속
- 다치 종속
- 조인 종속
정규화
함수적 종속성을 해소해 이상현상을 없애는 것
1차 - 모든 속성은 반드시 하나의 값을 가져야 함
2차 - 부분함수 종속을 제거 (모든 속성은 PK에 종속되어야 한다)
3차 - 이행함수 종속을 제거 (PK가 아닌 속성 간에는 종속될 수 없음)
4차 - 다치종속을 제거(관련있는 속성끼리 테이블을 분리해라)

철수의 취미 : 축구,독서
철수의 자격증 : 정보처리기사, SQLD


이름		취미		자격증
철수		축구		정보처리기사
철수		축구		SQLD
철수		독서		정보처리기사
철수		독서		SQLD
철수		등산		정보처리기사
철수		등산		SQLD

이름	취미
철수	축구
철수	독서
철수 	등산

이름	자격증
철수	정보처리기사
철수	SQLD

5차 - 조인종속을 제거(쪼개진 테이블을 JOIN을 했을 때 원래 결과가 나와야한다)

논리 데이터모델 검증하기
- 필요한 데이터가 빠지지 않았는지(입력,출력,연계 데이터가 모두 있는지 확인)
- 현재 시스템의 데이터 구조를 제대로 파악했는지(기존DB구조, 백업, 공통코드 확인)
- 사용자 요구사항이 반영되었는지(저장 용량, 백업, 보안, 데이터 전환)
- 데이터 흐름이 명확한지(업무 흐름에 따라 어떤 데이터가 생성,조회,수정,삭제되는지)
- 데이터 모델이 적절한지(엔티티,속성,관계,중복,무결성 검사)
- 접근권한과 통제가 명확한지(누가 어떤 데이터를 볼 수 있고, 수정할 수 있는지)

2. 물리데이터 저장소 설계하기
반정규화
- 조회 성능을 높이기 위해 데이터를 중복하거나 테이블 구조를 바꾸는 설계 방법
반정규화 유형
- 중복테이블 추가(집계, 진행상태,특정 컬럼을 포함)
- 테이블 조합(같이 사용되는 두 개 이상의 테이블을 하나로 합침)
- 테이블 분할(수평, 수직분할)
- 테이블 제거

물리데이터 모델링 하기
엔티티 -> 테이블
속성 -> 컬럼
UID -> 기본키(PK)로 변환
관계 -> 외래키(FK)
컬럼의 타입과 길이를 정의(프로젝트 할 때 정한 DBMS를 따라가면 된다.)
ORACLE
- 문자열 : CHAR, VARCHAR2(길이)
- 숫자 : NUMBER(자리수,소수점)
- 날짜 : DATE

MYSQL
- 문자열 : VARCHAR(길이)
- 숫자 : INT, DECIMAL
- 날짜 : DATE, DATETIME

반정규화 적용 여부 검토

3. 물리 데이터 저장소 구성
- 설계에 따라 데이터 저장소에 실제 데이터가 저장될 물리적 공간 구성

테이블 제약조건
- 테이블간 관계가 설정되었을 때 데이터가 잘못 저장된거나, 관계가 깨지는 것을 막기 위한 규칙

기본키 테이블의 데이터가 삭제 될 때
- CASCADE : 기본키의 데이터가 삭제되면 참조하는 데이터도 삭제
- RESTRICT : 데이터가 참조되고 있다면 기본키의 데이터는 삭제할 수 없다
- NULLIFY : 기본키의 데이터가 삭제되면 참조키 데이터를 NULL로 바꿈

사용법
CONSTRAINT 외래키명 FOREIGN KEY (컬럼명)
REFERENCES 테이블명(컬럼명)
ON DELETE 제약조건

기본키 테이블의 데이터가 수정 될 때
- CASCADE : 기본키의 데이터가 삭제되면 참조하는 데이터도 수정
- RESTRICT : 데이터가 참조되고 있다면 기본키의 데이터는 수정할 수 없다
- NULLIFY : 기본키의 데이터가 수정되면 참조키 데이터를 NULL로 바꿈

사용법
CONSTRAINT 외래키명 FOREIGN KEY (컬럼명)
REFERENCES 테이블명(컬럼명)
ON UPDATE 제약조건

인덱스 설계
적용되는 컬럼의 선정
분포도가 10 ~ 15%인 컬럼에 사용하는것이 일반적임
분포도 = (1/컬럼값의 종류) X 100

분포도가 범위 이상이라도 부분 처리를 목적으로 하는 경우
조회 및 출력 조건으로 사용되는 컬럼인 경우
기본키(PK)와 유일키(UNIQUE)는 인덱스가 자동으로 생성된다

분포도가 좋은 컬럼은 단독 인덱스로 생성한다.
자주 조합되어 사용되는 컬럼은 결합 인덱스로 사용

수정이 빈번하지 않은 컬럼으로 선정

뷰
뷰 속성
REPLACE ( CREATE OR REPLACE 뷰이름)
- 뷰가 이미 만들어져 있다면 기존 뷰를 새로운 내용으로 다시 생성하는 속성

FORCE
- 기본테이블이 없어도 뷰를 생성하는 속성
- 대신 뷰를 사용하려고 하면 에러 발생

NOFORCE
- 기본테이블이 없으면 뷰를 만들지 마라

WITCH CEHCK OPTION
- 뷰의 조건을 만족하는 행만 INSERT 또는 UPDATE 할 수 있다.

WITH READ ONLY
- 뷰를 조회 전용으로 만드는 속성

뷰 설계시 고려사항
- 복잡한 뷰는 성능이 느려질 수 있다.
- 뷰 안의 SELECT 조건도 인덱스를 고려해야 한다.

클러스터 설계
- 관련된 데이터를 물리적으로 가까운 위치에 저장하여 조회 성능을 높이는 기법

인덱스 : 목차
ㄴ 특정 행의 검색이 빠르다
클러스터 : 물리적으로 모아둠
ㄴ 범위 조회, 반복 조인 조회가 빠르다.

클러스터 만드는법
1. 클러스터 생성
CREATE CLUSTER 클러스터이름(
	컬럼명 타입;
)

2. 클러스터 인덱스 생성
CREATE INDEX 인덱스명
ON CLUSTER 클러스터명;

3. 클러스터를 사용하는 테이블 만들기
CREATE TABLE 테이블명(
	...
)
CLUSTER 클러스터이름(컬럼)

MYSQL은 오라클처럼 직접 만들지는 않는다.
PK를 기준으로 자동으로 클러스터형 인덱스만들어진다.

적용기준
- 분포도가 넓을 수록(수치가 낮은것) 유리한 기법
- 클러스터는 여러 블록이상의 테이블에 적용한다.(작은 테이블에는 효과가 크지 않다.)
- 대량의 범위를 자주 조회할 때 적용
- 여러 테이블이 빈번히 조회될 때

주의사항
- 검색은 빨라지지만 입력,수정,삭제는 느려질 수 있다.

파티션
- 하나의 큰 테이블을 내부적으로 여러 조각으로 나눠 저장하는 기술

파티션 종류
범위 분할
- 사용자가 데이터를 구간별로 나눈다.
- 데이터가 각 파티션별로 골고루 들어가있지 않을 수도 있다.
- 조회 범위를 줄일 수 있다.
- 오래된 데이터를 관리하기 쉽다.
- 주문,로그, 게시글, 이력데이터

해시 분할
- DB가 계산해서 각 파티션에 골고루 분배
- 해시값 % 파티션 개수 -> 어떤 파티션에 넣을지 결정
- 회원, 사용자,고객 데이터 -> ID기반의 조회가 많은 시스템
조합 분할
- 파티션 기준을 2개 이상 조합해서 데이터를 나누는 방식
- 먼저 큰 기준으로 한 번 나누고 그 안에서 다시 작은 기준으로 한 번 더 나누는 방식
- 1차 파티션만으로 부족할 때
- 날짜 조건과 고객 조건을 함게 자주 사용할 때
- 특정 연도나 월에 데이터가 몰리는 문제를 줄일 수 있다.

파티셔닝 순서
- 파티션 종류 결정(범위,해쉬,조합)
- 파티션 키 선정(데이터를 나눌 기준이 되는 컬럼)
- 파티션 수 선정

디스크 구성 설계
- 데이터를 어디에, 어떻게 저장할 것인지 계획하는 작업
- 정확한 용량을 산정하여 디스크 사용의 효율을 높인다.
- 업무량이 집중되어있는 디스크를 분리하여 설계함으로써 집중화된 디스크에 대한 입출력 부하를 분산한다.
- 입출력 경합을 최소화하여 데이터의 접근 성능을 향상시킨다.

물리 데이터 모델 구성하기
테이블 이름 정하기, 컬럼 이름 길이 정하기, 기본키 외래키 정하기

요구사항이 잘 반영됐는지 확인
DBMS 선택(ORACLE, MYSQL, MONGODB...)을 하고 학습

수행순서
다양한 오브젝트를 설계(테이블,제약조건,인덱스,뷰,클러스터,파티션,테이블스페이스)
ㄴ 제약조건 설계 : 데이터 무결성 보장
ㄴ 인덱스 설계 : 검색 속도 향상
ㄴ 뷰 설계 : 재사용성과 보안
ㄴ 클러스터 설계 : 물리적 저장 최적화
ㄴ 파티션 설계 : 대용량 데이터 관리
ㄴ 디스크 구성 설계 : I/O 성능 최적화

4. 데이터 조작 프로시저 작성하기
DDL : CREATE, DROP, ALTER
ㄴ 데이터베이스 객체 조작
DML : SELECT, INSERT, UPDATE, DELETE
ㄴ 테이블 내부의 데이터를 조작
DCL : GRANT, REVOKE, ROLE
ㄴ 유저에게 데이터베이스의 접근 권한과 보안을 관리하는 명령어

GRANT 권한 ON 테이블 TO 유저명;

REVOKE 권한 ON 객체명 FROM 사용자명;

ROLE : 여러 권한을 하나의 그룹처럼 묶어서 관리하는 기능

CREATE ROLE 역할이름;

ROLE에게 권한 부여
GRANT SELECT, INSERT, UPDATE ON 테이블 TO 역할이름;

사용자에게 ROLE 부여
GRANT 역할이름 TO 유저이름;

DCL 사용시 주의사항
최소 권한 원칙
- 사용자에게 꼭 필요한 권한만 부여해야 한다.

ALL PRIVILEGES 남용 금지
- 모든 권한을 부여하는 것은 보안상 위험할 수 있다.

운영환경에서는 권한 관리 기록필요
- 누가 언제 어떤 권한을 부여했는지 기록

TCL : COMMIT, ROLLBACK, SAVEPOINT
- 트랜잭션 제어 명령어
- 데이터 무결성 유지
- 작업단위로 관리
- 복구 가능 (오류 발생 이전 상태로 돌아갈 수 있다.)
- 실무에서 매우 중요하다

절차형 데이터 조작 프로시저 및 저장형 객체

절차형 데이터 조작 프로시저
- 여러 SQL과 조건문,반복문, 예외 처리를 하나의 프로그램처럼 작성하는 방식
- 여러 작업을 순서대로 처리하는 경우가 많고 SQL한문장으로는 처리하기 어렵기 때문에 절차형 SQL을 사용한다.

PL/SQL
- ORACLE에서 제공하는 절차형 SQL 언어
- 변수, 상수, 조건문, 반복문, 예외 처리, 커서, 프로시저, 함수, 트리거와 같은 프로그래밍 기능을 추가한다.

BEGIN
	SQL 작성
END;
/

일반 PL/SQL문에서 변수 선언
DECLARE
	v_name VARCHAR2(50);
	v_age NUMBER;
BEGIN

END;
/

프로시저 안에서 변수 선언
CREATE OR REPLACE PROCEDURE ...
IS
	v_name VARCHAR2(50);
	v_age NUMBER;
BEGIN

END;

IF문
DECLARE
	v_score NUMBER := 85;
BEGIN
	IF v_score >= 60 THEN
		DBMS_OUTPUT.PUT_LINE('합격');
	ELSE
		DBMS_OUTPUT.PUT_LINE('불합격');
	END IF;
END;

LOOP : 같은 작업을 반복할 때 사용
DECLARE
	v_score NUMBER := 85;
BEGIN
	LOOP
		DBMS_OUTPUT.PUT_LINE(v_score);
		v_score := v_score + 1;
		EXIT WHEN v_score > 90
	END LOOP;
END;

ERROR 처리
EXCEPTION을 이용하여 오류를 처리할 수 있다.

DECLARE
    v_name member.name%TYPE; -> member테이블의 name컬럼의 타입과 길이를 쓰겠다.
BEGIN
    SELECT name
    INTO v_name
    FROM member
    WHERE member_id = 999;

   DBMS_OUTPUT.PUT_LINE(v_name);

   EXCEPTION
   WHEN NO_DATA_FOUND THEN
   DBMS_OUTPUT.PUT_LINE('조회된 회원이 없다')
END;
/

Stored Function
- 값을 계산하고 결과값을 반환하기 위해 사용하는 함수

CREATE OR REPLACE FUNCTION 함수명
매개변수
RETURN 반환타입
IS
지역변수
BEGIN
내가 하고싶은 명령
RETURN 반환값;
END;

Stored Procedure
- function은 값을 반환하는데 초점이 있다면, procedure는 작업 수행에 초점이 있다.

in 매개변수 -> 외부에서 값을 받아오는 매개변수
out  매개변수 -> 외부로 값을 내보내는 매개변수
inout 매개변수 -> 값을 받아와서 처리한 다음에 내보내는 매개변수

create or procedure 프로시저명(
	매개변수
)
IS
	지역변수
BEGIN
	트랜잭션
END;

바인드 변수(전역변수)
VARIABLE 변수명 타입;

OUT매개변수가 있는 경우
EXEC 프로시저명(:변수명);

INOUT매개변수
EXEC :변수명 := 값;
EXEC 프로시저명(:변수명);

Stored Package
- 서로 관련있는 PL/SQL 프로시저와 함수들의 집합
- 관련 기능을 하나의 폴더처럼 묶어놓는 객체

패키지 선언부
- 외부에 공개할 프로시저, 함수, 변수 등을 선언
- 패키지에 포함될 요소를 미리 선언하는 부분

CREATE OR REPLACE PACKAGE 패키지명 IS
	변수 선언;
	예외 선언;
	커서 선언;
	PROCEDURE 프로시저명(매개변수);

	FUNCTION 함수명(매개변수) RETURN 자료형;
END;

CREATE OR REPLACE PACKAGE member_pkg IS
	v_count NUMBER;  -- 변수

	CURSOR cur_member IS -- 커서
		SELECT * FROM member;

	e_not_found EXCEPTION; -- 예외처리

	PROCEDURE get_member(p_member_id IN NUMBER); -- 프로시저

	FUNCTION get_count RETURN NUMBER; -- 함수
END;

패키지 본문
- 선언된 프로시저와 함수의 실제 실행 내용을 작성
- 실제 코드

CREATE OR REPLACE PACKAGE BODY 패키지명 IS
	변수 선언절;
	커서 선언절;
	예외 선언절;
	PROCEDURE 선언절;
	FUNCTION 선언절
END 패키지명;

CREATE OR REPLACE BODY member_pkg IS

	PROCEDURE get_member( -- 프로시저 내용 작성
		p_number_id IN NUMBER
	)
	IS
		v_name member.name%TYPE;
	BEGIN
		SELECT name
		INTO v_name
		FROM member
		WHERE member_id = p_member_id;

		DBMS_OUTPUT.PUT_LINE(v_name);

	EXCEPTION
		WHEN NO_DATA_FOUND THEN
			RAISE e_not_found;
		
		WHEN e_not_found THEN
			DBMS_OUTPUT.PUT_LINE('없다');
	END get_member;

	FUNCTION get_count -- function 작성
	RETURN NUMBER
	IS
	BEGIN
		SELECT COUN(*)
		INTO v_ount -- 선언부쪽에 작성한 변수를 몸체쪽에서 사용
		FROM member;

		RETURN v_count;
	END get_count;
END member_pkg;
	
패키지 사용하기
패키지명.프로시저명();
member_pkg.get_member();
패키지명.함수명;
member_pkg.get_count();

트리거
- INSERT,UPDATE,DELETE문이 테이블에 대해 실해됐을 때 자동으로 수행되는 프로시저

CREATE OR REPLACE TRIGGER 트리거명
[시점] [이벤트] ON 테이블명
[FOR EACH ROW]  -- 영향을 받은 행의 수만큼 실행
[WHEN 조건]
DECARE
    변수 선언
BEGIN
    실행할 명령
END;

CREATE OR REPLACE TRIGGER trg_member_insert
AFTER INSERT ON member
FOR EACH ROW
BEGIN
    DBMS_OUTPUT.PUT_LINE(:NEW.name || ' 회원이 등록되었습니다.');
END;

데이터 조작 프로시저 개발
1. java <-> dbms를 연결해라
ㄴ dbms 종류에 따라서 jdbc 선택 잘 하시오
ㄴ Connection, PreparedStatement, ResultSet
2. 저장소로부터 데이터를 읽어오는 SELECT문을 잘 작성해라
3. 조회 결과를 받아서 ResultSet으로 받아서 Java에서 처리해라

단위 테스트(Unit Test)란?
프로그램의 가장 작은 단위(함수,메섣, 클래스 등)를 개별적으로 테스트 하는 것

CREATE OR REPLACE PROCEDURE test_proc
IS
    v_num NUMBER := 10;
BEGIN
    DBMS_OUTPUT.PUT_LINE('시작');
    DBMS_OUTPUT.PUT_LINE('v_num = ' || v_num);
    v_num := v_num + 5;
    DBMS_OUTPUT.PUT_LINE('증가 후v_num = ' || v_num);
    DBMS_OUTPUT.PUT_LINE('종료');

END;

SQL 버퍼
- 최근 입력한 SQL문장을 임시로 저장해두는 메모리 공간이다.

1. 파일 명령어
SAVE - 현재 SQL 문장을 저장
SELECT * FROM MEMBER;

SAVE member.sql

GET - 저장된 sql 불러오기
GET member.sql

SPOOL - 실행 결과를 파일에 저장
SPOOL result.txt
SELECT * FROM member;
SPOOL OFF

2. 편집명령어
L(List) - 버퍼 내용을 확인
L
결과
1. select *
2. from member

A(Append) - 문장 뒤에 추가
SELECT * FROM MEMBER -> 버퍼에 존재
A WHERE member_id = 1

결과
SELECT * FROM MEMBER WHERE member_id = 1;

C(Change) - 문자열 변경

현재 버퍼 select name from member;

C/name/member_name

SELECT member_name from member;

DEL - 현재 줄을 삭제한다.
현재 버퍼
SELECT * 
FROM MEMBER 
WHERE member_id = 1;

3번째 줄을 선택

DEL

SELECT *  
FROM MEMBER;





sql> SELECT * 
FROM MEMBER 
WHERE member_id = 1;

sql> L

1. SELECT *
2. FROM MEMBER
3. WHERE member_id = 1;

SQL > 3 엔터 -> 세번째 줄 선택
3* WHERE member_id = 1
SQL > DEL -> 선택한 줄을 삭제

I(INSERT) - 현재 줄 다음에 새로운 줄을 삽입
sql> L

1 SELECT *
2 FROM MEMBER

SQL > 2
2* FROM MEMBER

SQL > I
3 WHERE member_id = 1
4 . -> .은 insert 모드를 빠져나가는 기호

SQL > L
1 SELECT *
2 FROM MEMBER
3 WHERE member_id = 1

RUN - 버퍼 내용을 출력한 후 실행한다.
SQL > RUN
1 SELECT *
2 FROM MEMBER

ID	NAME
--------------
1	홍길동
2	김철수

/ : 버퍼 내용을 바로 실행

SQL > BEGIN
2	DBMS_OUTPUT.PUT_LINE('HELLO');
3 END;
4 /

START - SQL 파일 실행
SQL > START member.sql

SET SERVEROUTPUT - PL/SQL 출력 결과를 표시한다.
SQL > SET SERVEROUTPUT ON
SQL > BEGIN
2	DBMS_OUTPUT.PUT_LINE('안녕하세요');
3    END;
4 /

@ 파일명 - 파일을 실행한다.
SQL > @test.sql

DEFINE - 변수를 선언한다
SQL > DEFINE id = 1
SQL > SELECT *
2	FROM member
3	WHERE member_id = &id;

ACCEPT - 사용자 입력을 받는다.
SQL > ACCEPT p_id NUMBER PROMPT '회원번호 입력 : '

회원번호 입력 : 1

SQL > SELECT *
2	FROM member
3	WHERE member_id = &p_id;

형식 명령어
COLUMN - 컬럼의 출력 형식을 지정한다.

COLUMN 컬럼명 FORMAT 형식

ID	NAME
----	--------
1	홍길동
2	김철수

A20 : 데이터 출력의 너비를 20칸으로 지정해라
SQL > COLUMN name FORMAT A20

ID	NAME
----	-----------------------------
1	홍길동
2	김철수

TITLE - 조회 결과의 맨 위에 제목을 출력한다.

SQL > TITLE '회원 목록'

SQL> SELECT member_id, name
2	FROM member;

회원 목록
ID	NAME
---	-------
1	홍길동
2	김철수

BTITLE - 조회 결과의 맨 아래 제목을 출력한다.

SQL > BTITLE '조회 종료'

SQL> SELECT member_id, name
2	FROM member;

ID	NAME
---	-------
1	홍길동
2	김철수

조회 종료

BREAK - 같은 값이 반복될 때 중복 출력을 생략하거나 소계를 출력하기 위해 사용하는 명령어

SQL > BREAK ON department_name

SQL> SELECT department_name,
2		employee_name,
3		salary
4	FROM employees
5	ORDER BY department_name;

department_name		employee_name		salary
--------------------		------------------		-----------
개발팀			김철수			3000
				이영희			3500
				박민수			4000
인사팀			홍길동			2800
				최지훈			3200








