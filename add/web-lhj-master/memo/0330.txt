참조용 클래스(객체를 만들기 위한 클래스)
- main 메서드가 없음
- 필드
- 생성자(호출했을 때 메모리에 객체를 올려주는 기능)
ㄴ 객체를 생성했을 때 필드를 초기화 하는 역할
정의하지 않으면 컴파일러가 기본생성자를 만들어서 사용

사용자가 직접 정의하면 컴파일러가 만든 기본생성자는 사용되지 않는다.

메서드처럼 매개변수를 가질 수 있다.

오버로딩(overloading)
- 같은 이름으로 서로 다른 개수, 타입의 매개변수를 각각 처리하는 방법

- 반환형은 달라도 상관 없음
- 메서드(or생성자)명은 같아야 한다.
- 매개변수의 개수가 다르거나, 타입이 다르거나, 순서가 달라야 한다.

자동차 -> 클래스
핸들 -> 클래스
타이어 -> 클래스
엔진 -> 클래스

자동차 클래스가 핸들, 타이어, 엔진객체를 받을 수 있도록 필드로 갖는다.

기본요금 : 10,000원(상수)
추가 요금 : 시간당 2,000(상수)
사용시간을 키보드에서 입력받아 총 금액이 얼마인지 출력하기

할인률을 결정하는 Discount클래스
할인률을 상수로 가지고 있다.
RATE_HIGHT = 0.2;
RATE_MID = 0.1;
RATE_LOW = 0.05;

금액에 따라서 할인률을 결정하는 disCountRate메서드 만들기
금액이 10만원 이상이면 0.2
5만원 이상이면 0.1
5만보다 적으면 0.05 적용

최종 금액을 계한하는 calculatePrice메서드 작성
- 할인률이 적용된 금액을 반환하세요

DiscountMain에서 금액을 키보드에서 입력받아서 할인 적용금액 구하기

-----------------------------------------------------------------------
- 메서드(여러개 정의가 가능하다)
접근제한자 반환타입 메서드명(매개변수){

}

메서드(계산기)
정의(기능을 만드는것)
ㄴ 숫자 두개와 연산자를 받을 준비
호출
ㄴ 숫자와 연산자를 전달하여 연산결과를 돌려받는다.

return
매개변수, 메서드 내부에서 정의된 변수는 지역변수이다.
메서드가 호출돼서 실행한다음 종료가 되면 메모리에서 삭제된다.

만약, 메서드 밖에서 연산결과를 사용하고 싶다면 return을 통해 값을 밖으로 꺼내야 한다.

메서드명
- 메서드를 호출할 때 사용하는 이름
- 첫글자는 소문자로 작성
-----------------------------------------------------------------------
접근제한자(Access Modifier)
public : 프로젝트 내에서는 어디서든지 사용 가능
default : 같은 패키지내의 다른 클래스 어디서든지 사용 가능
private : 현재 클래스에서만 사용가능
protected

필드/생성자/메서드/클래스에 붙힐 수 있다.

Student 클래스를 만들기
필드
- 문자열 타입의 이름
- 정수 타입의 나이

모든 필드는 private으로 선언
setter / getter를 각각 구현

Main 클래스에서 학생 객체를 만든 후 값을 세팅하고 콘솔에 값을 출력하기

나이에 0 미만의 값이 들어오면 "잘못된 나이입니다" 출력하기

User클래스를 작성한다.
필드
- 문자열 :  username
- 문자열 : password
- 문자열 : email

setter&getter를 이용해서 필드의 값을 세팅하거나 가져올 수 있다.
- username은 4글자 이상
ㄴ"아이디는 4글자 이상이어야 합니다."
- password는 6글자 이상
ㄴ"비밀번호는 6글자 이상이어야 합니다.
- email은 @를 포함해야 한다.
ㄴ "이메일 형식 오류"

디자인 패턴
프로그래밍을 하다보면 비슷한 문제가 반복적으로 나타나는 경우가 있다.
- 객체를 하나만 만들어야 하는 경우
- 객체 생성이 너무 복잡한 경우
- 기능은 비슷한데 구현 방법이 여러개인 경우
- 객체들끼리 너무 강하게 연결되어 있어 수정이 어려운 경우

개발자들이 오랜 시간 동안 많이 쓰인 설계 방법을 정리해둔 것이 디자인 패턴이다.

출석관리 프로그램이 있다.
AttendanceManager클래스가 있다.
- 학생 출석 수를 저장하는 count변수가 있다.
- addAttendance() 메서드를 호출하면 출석 수가 1증가한다.
- getAttendance()로 현재 출석수를 반환한다.
- 프로그램 전체에서 출석관리 객체는 하나만 존재해야 한다.

상속의 특징
하나의 부모는 여러 클래스한테 상속할 수 있다.
하나의 자식이 여러 클래스를 상속받는건 불가능하다.

필기구 클래스를 상속으로 설계하시오
- 여러 종류의 필기구를 관리하는 프로그램
- 필기구에는 공통적으로 남은양(amount)가 있고, 종류에 따라 추가 속성이 있을 수 있다.

Pen 클래스
- 모든 펜의 부모 클래스이다.
- 남은양을 저장하는 amount 필드를 가진다.

SharpPen클래스
- Pen을 상속받는다.
- 샤프심의 굵기를 나타내는 width 필드를 가진다.

BallPen클래스
- Pen클래스를 상속받는다.
- 볼펜의 색을 나타내는 color필드를 가진다.

FountainPen클래스
- BallPen클래스를 상속받는다.
- 만년필은 잉크를 다시 채울 수 있으므로 refill(int n)메서드를 작성한다.
- refill 메서드는 남은 양을 n으로 변경한다.
















