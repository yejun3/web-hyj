http://naver.me/x7BmryYm

SQL * PLUS : 프로그램
- 오라클에서 제공해주는 명령어 기반 프로그램
- 오라클 데이터베이스에 접속해서 SQL문을 작성하고 실행할 수 있다.

SQL 버퍼(Buffer)의 사용
- 임시 저장 공간이 존재한다.
- 이전에 실행했던 SQL이 저장이 된다.

데이터 조작 프로시저 테스트

디버깅을 잘 해야 한다.
디버깅 : 오류의 원인을 찾아 수정하는 과정

프로시저를 실행했는데 예상한 결과와 다르다 -> 그 이유를 찾아야 함

EX)
특정 사원의 급여를 증가시키는 프로시저
예상 결과 : 급여가 10% 증가
실제 결과 : 급여가 변경되지 않음

1. 입력값이 제대로 들어갔는지
2. SELECT결과는 존재하는지
3. UPDATE 조건은 맞게 썼는지
4. COMMIT이 필요한지(INSERT,UPDATE,DELETE)
5. 예외가 발생하지는 않았는지

SHOW ERRORS

SQL * PLUS 명령어 유형
파일
SAVE - 버퍼에 들어있는 명령어를 파일로 저장
GET - 파일의 내용을 SQL 버퍼로 불러온다.
SPOOL - 실행 결과를 파일로 저장한다.
편집
L(LIST) - 현재 SQL 버퍼 내용을 확인한다.
A(APPEND) - 현재 줄 끝에 내용을 추가한다.
C(CHANGE) - 문자열을 변경한다.
I(INSERT) - 새로운 내용을 추가한다.
DEL - 현재 줄을 삭제한다.
실행
RUN - 버퍼의 SQL을 실행한다.
/ - SQL 퍼버의 내용을 바로 실행한다.
START - SQL파일을 실행한다.
@ - SQL파일을 실행한다.
환경
SET SERVEROUTPUT - 출력한 내용을 화면에 표시하도록 설정
형식
COLUMN - 출력 형식 지정
TTITLE - 상단 제목 출력
BTITLE - 하단 제목 출력
대화
DEFINE - 변수를 선언
ACCEPT - 사용자 입력을 받는다.

PL/SQL 테스트를 위한 SQL * PLUS의 활용
- PL/SQL은 변수,조건문,반복문, 예외 처리 등을 사용해서 여러 작업을 절차적으로 처리할 수 있다.
- 단순히 SQL 실행 결과만 보는게 아니라, 내부 처리 과정이 맞는지도 확인해야 한다는 것이다.

테스트 항목
실행이 되는가 : 문법 오류 없이 실행이 되는지 확인
출력이 맞는가 : 예상한 값이 출력이 되는지
데이터가 바뀌었는가 : INSERT, UPDATE, DELETE 결과를 해야 한다. -> COMMIT
오류가 없는가 : SHOW ERRORS를 통해 오류를 확인
조건별 결과가 맞는지 : 정상값, 잘못된 값, NULL값 등을 넣어 결과를 확인한다.

DBMS_OUTPUT 패키지
- PL/SQL 처리 결과를 화면에 출력하기 위해 사용
- PUT_LINE()메서드가 메시지 끝에 줄바꿈을 포함하여 출력
- 프로시저나 함수 사이에 단계별 출력문을 넣어서 테스트를 한다.

프로시저 테스트
1. 실행전 데이터 확인
2. 프로시저 실행
3. 실행 후 데이터 확인
4. 의도대로 작동하지 않으면 디버깅

데이터 조작 프로시저 테스트 수행
- 수행 기준
설계 문서와 개발 기준을 보고 테스트 해야 한다.

**문제1**

- 다음 PL/SQL 코드를 실행했지만 화면에 아무 메시지도 출력되지 않았다.
    
    ```sql
    BEGIN
        DBMS_OUTPUT.PUT_LINE('테스트 성공');
    END;
    /
    ```
    
- 출력 결과를 확인하기 위해 실행 전에 입력해야 하는 `SQL*Plus` 명령어를 작성하시오.

SET SERVEROUTPUT ON

**문제2**

- 다음은 Oracle SQL*Plus에서 프로시저를 생성한 결과이다.
    
    ```
    Warning: Procedure created with compilation errors.
    ```
    
1. 이 메시지의 의미를 설명하시오.
- 프로시저가 만들어졌는데 컴파일 과정에서 에러가 발생함
2. 오류의 원인을 확인하기 위해 사용해야 하는 `SQL*Plus` 명령어를 작성하시오.
- SHOW ERRORS

**문제3**

- 다음은 급여를 수정하는 프로시저이다.
    
    ```sql
    CREATE OR REPLACE PROCEDURE update_sal
    IS
    BEGIN
    	UPDATE emp
    	SET sal= sal+100;
    END;
    /
    ```
    
- 프로시저가 정상적으로 동작하는지 테스트하기 위한 절차를 순서대로 작성하시오.

1. 급여 확인
SELECT EMPNO, SAL FROM EMP;

2. EXECUTE UPDATE_SAL

3. SELECT EMPNO, SAL FROM EMP;

문제4
- 다음 SQL*Plus 명령어의 용도를 작성하시오.
    
    ```sql
    SPOOL result.txt
    ```
---

**문제5**

다음 코드를 실행하였다.

```sql
VARIABLE v_count NUMBER;

EXEC :v_count :=50;

PRINT v_count;
```

1. VARIABLE 명령어의 역할을 설명하시오.
2. PRINT 명령어의 역할을 설명하시오.

**문제6**

다음 저장 객체들의 차이점을 설명하시오.

- Stored Function
- Stored Procedure

---
Function
- 반드시 값을 반환한다.
- RETURN을 사용한다
- 계산 결과나 특정 값을 반환할 때 사용한다.

Procedure
- 특정 작업을 수행하기 위한 저장 프로그램
- 반환값이 필수는 아니다
- 데이터 입력,수정, 삭제 등의 작업에 주로 사용한다.

**문제7**

다음 조건을 만족하는 패키지 실행 코드를 작성하시오.

조건
- DBMS_OUTPUT 출력 결과가 보이도록 설정
- 패키지명 : EMP_INFO
- 프로시저명 : ALL_EMP_INFO


SET SERVEROUTPUT ON;
EXECUTE EMP_INFO.ALL_EMP_INFO;


**문제8**

- 개발자가 다음 명령을 실행하였다.
    
    ```sql
    SPOOL result.txt
    
    SELECT *
    FROM emp;
    ```
    
- 그런데 result.txt 파일이 생성되었지만 내용이 계속 추가되고 있었다.
- 이러한 현상이 발생한 이유를 설명하고 해결 방법을 작성하시오.

SPOOL OFF를 작성하지 않았기 때문에 다른 명령들도 계속 추가된다.
SPOOL OFF


**문제9**

- 개발팀은 다음 기능을 구현하려고 한다.
    
    ```
    회원이 삭제되면 삭제 시간과 회원번호를 자동으로 기록한다.
    ```
    
- 가장 적절한 저장 객체를 선택하고 이유를 설명하시오.

TRIGGER

삭제가 발생할 때 자동으로 실행되어야 하기 때문이다.
Trigger는 특정 이벤트 발생시 자동 실행 된다.


**문제10**

- 다음 프로시저는 사원 급여를 수정한다.
    
    ```
    update_sal(empno)
    ```
    
- 현재 학생이 작성한 테스트 케이스는 다음과 같다.
    
    ```
    사원번호 7369 입력
    → 정상 실행 확인
    ```
    
- 위 테스트 케이스가 부족한 이유를 설명하고 추가로 필요한 테스트 케이스를 2개 이상 작성하시오.

정상 입력 테스트만 했다.
예외 상황에 대한 검증이 없다.

존재하지 않는 사원번호
NULL 입력
음수 사원번호
문자열 입력


SQL 처리 흐름
1. 구문 분석
- 사용자 요청한 SQL문이 데이터베이스에서 처음 사용된 문장인지, 이미 사용된 문장인지를 공유 풀 영역을 검색하여 확인

※공유 풀 영역
- SQL 실행 계획을 저장하는 메모리 영역(SQL문이 보관되어 있다)

사용된적 있는 문장 -> 공유 풀에서 꺼내서 쓰면 됨(구문분석 필요X)
처음 사용되었다 -> 구문분석을 실시

구문분석
- SQL문을 데이터베이스가 이해할 수 있도록 분석하는 작업
예시
SELECT NAME
FROM MEMBER
WHERE MEMBER_ID = 1;

SELECT 문법이 올바른가
MEMBER 테이블이 존재하는가
NAME 컬럼이 존재하는가
MEMBER_ID 컬럼이 존재하는가
사용자가 MEMBER 테이블을 조회할 권한이 있는가?

- 옵티마이저는 SQL문을 가장 빠르게 데이터를 검색해줄 수 있는 실행 계획을 찾는다.

※ 옵티마이저
- SQL을 가장 효율적으로 실행할 방법을 결정하는 데이터베이스 내부 기능이다.


2. 실행
구문분석이 끝난 SQL을 실제로 실행하는 단계
- 서버 프로세스는 메모리 영역의 데이터베이스 버퍼 캐시 영역을 검색하여 해당 테이블의 데이터가 다른 사용자의 다른 SQL문에 의해 존재하는지 검색

※데이터베이스 버퍼 캐시
- 실제 테이블 데이터를 저장하는 영역

버퍼 캐시 영역에 있으면 그대로 사용

없으면 해당 테이블의 데이터파일에서 데이터를 읽고 버퍼캐시에 저장

INSERT, UPDATE, DELETE와 같은 문장이러면 데이터 버퍼 캐시 영역에서 새로운 데이터로 변경, 삭제 또는 입력을 한다.


3. 추출
실행 결과를 사용자에게 보내주는 단계
서버 프로세스는 데이터베이스 버퍼 캐시 영역에서 관련 테이블 데이터를 읽어서 사용자가 요청한 클라이언트로 보내준다.

SELECT문을 실행하는 경우에만 추출단계가 실행된다.
-------------------------------------------------------------------------------
SQL 작성시 고려사항
- 개발자는 SQL 특성을 충분히 이해하여 SQL문을 적절히 구사할 수 있는 능력을 갖춰야 한다.
- 사용자가 SQL 작성 시, 옵티마이저가 실행 계획을 수립한 후 실행되는 일련의 과정을 이해하고 작성해야 한다.
- 구문분석 단계 시 옵티마이저가 수립한 실행 계획에 다라 엄청난 수행 속도 차이가 발생할 수 있다.
- 특정 SQL이 실행될 때 옵티마이저에 의해 수립된 실행 계획은 제어하기 어렵지만, 옵티마이저가 비상식적으로 동작한다면, 이를 추적하여 개발자가 원하는 실행 계획으로 동작될 수 있도록 조정하는 과정이 필요하다.
- 개발자는 옵티마이저가 정상적인 실행 계획을 수립할 수 있도록 종합적이고 전략적인 포인트를 SQL에 부여하여 작성하여야 한다.
- 좋은 SQL은, 추출되는 결과를 추론하여 SQL을 집합적으로 접근하여 작성하여야 한다.

SQL 성능 최적화를 위한 유틸리티(프로그램)의 활용
- SQL의 문제점을 파악하고 개선하여 SQL 성능을 최적화하기 위해, TKPROF 및 EXPLAIN PLAN이라는 도구를 제공하고 있다.

TKPROF
SQL Trace 파일을 사람이 읽기 좋게 변환해주는 성능 분석 도구

SQLTRACE
- ORACLE 데이터베이스에서 SQL이 실제로 어떻게 실행되었는지 기록하는 기능
기록되는 정보
Parse
- SQL 분석 단계 정보
SQL 문법 분석
실행 계획 생성

Execute
- SQL 실행 단계 정보
실제 SQL 수행

Fetch
- 조회 결과 가져오기 정보
SELECT 결과 반환

SQL TRACE 활성화
ALTER SESSION SET SQL_TRACE = TRUE;

SQL 실행
SELECT * FROM EMP WHERE EMPNO = 7369;

분석이 끝나면 비활성화
ALTER SESSION SET SQL_TRACE = FALSE;

SQL TRACE 결과는 특정 폴더에 .trc파일 형태로 저장이 된다.

TKPROF로 보기좋게 변환하기

결과 분석하기

EXPLAIN PLAN의 활용
- 사용자들이 SQL문의 액세스 경로를 확인하여 성능 개선을 할 수 있도록 SQL문을 분석하고 해석하여 계획을 수립하고, 관련 테이블에 저장하도록 지원해주는 도구

테스트 조작 프로시저 성능 개선하기

1. 분석가에 의해 정의된 유가사항을 기준으로, 성능 측정 도구를 활용하여 데이터 조작 프로시저의 성능을 측정한다
- 응답 시간, 처리 건수, 리소스 사용량 등을 목표로 분석하고 테스트 대상 성능 기준을 정의한다.
- 성능 도구 설정, Trace 로그 수집 준비
- 테스트용 데이터를 사용하여 프로시저 실행하고, 실행결과 및 Trace 로그 수집
- 결과를 분석하여 지표를 검토한다.
2. 실데이터를 기반으로 텍스트를 수행하여 데이터 조작 프로시저의 성능에 영향을 주는 병목구간을 파악한다.
- 운영 데이터의 복사본 도는 유사 데이터를 준비하여 성능 테스트 환경 구성
- 대량 처리, 복합 조건, 동시성 등 다양한 상황을 반영하여 테스트 시나리오 작성
- pl/sql블록 실행 또는 SQL script 반복실행을 통해 성능 데이터 수집
- 병목 지점 식별
- 성능 저하 원인 보고서 작성
3. 테스트 결과와 정의된 요구사항을 기준으로 데이터 조작 프로시저의 성능에 따른 이슈를 해결한다
- 속도를 저하시키는 sql 구문을 재작성하여 성능을 개선한다.
- 실행 계획을 개선한다.
- 인덱스를 재구성한다
- 시스템 리소스 병목 구간을 조정한다.
- 개선 후 동일한 테스트로 효과를 검증한다.
- 성능 개선 결과를 문서화 하고 공유한다.

[문제 1] 
SQL 구문 분석(Parsing) 단계에서 옵티마이저가 생성한 실행 계획을 저장하는 Oracle의 내부 테이블 이름은 무엇인지 기술하시오.

plan_table

[문제 2] 
어플리케이션 성능 모니터링(APM) 도구 중, 단순히 서버의 CPU나 메모리 리소스를 감시하는 것을 넘어 사용자의 요청부터 DB 조회, 외부 API 호출까지의 전체 거래 과정을 추적하는 방식을 일컫는 용어를 기술하시오.

엔드 투 엔드

[문제 3] 
다음은 공유 풀(Shared Pool)과 데이터베이스 버퍼 캐시(Data Buffer Cache)에 대한 설명입니다. 대량의 회원을 조회하는 동일한 SQL문이 반복해서 실행될 때, 두 메모리 영역이 SQL 처리 흐름(구문 분석, 실행, 추출) 단계에서 각각 성능 최적화에 어떻게 기여하는지 메커니즘을 비교하여 서술하시오.

공유풀
- 이전에 실행된 sql문과 실행 계획을 임시 보관하므로, 동일한 SQL이 들어왔을 때 문법 검사, 오브젝트 확인, 실행 계획 수립 등의 작업을 생략한다.

버퍼캐시
- 디스크에서 한 번 읽어온 데이터를 메모리에 유지하므로, 동일한 데이터 조회 시 느린 디스크 파일 I/O를 발생시키지 않고 메모리에서 바로 데이터를 읽어와 수행 속도를 극적으로 향상시킨다.

[문제 4] 
APM 리소스 모니터링 화면에서는 CPU 20%, 메모리 40%로 매우 정상적인 수치를 보이고 있으나, 실제 사용자는 "주문 완료 화면이 넘어가는 데 8초 이상 걸린다"며 불편을 겪고 있습니다. 이 현상이 발생할 수 있는 원인을 엔드투엔드(E2E) 모니터링 관점에서 분석하고, 성능 개선을 위해 개발자가 확인해야 할 내부 유틸리티 2가지를 제시하시오.

원인 분석
- 리소스는 여유가 있으나 주문 처리 트랜잭션 내부에서 병목이 발생했다.
E2E 관점에서 볼 때 외부 결제 API 호출 응답 지연, 문자 발송 서버의 지연, 혹은 특정 SQL의 락(Lock)이나 비효율적인 실행 계획으로 인해 내부 프로세스가 대기 상태에 빠졌을 가능성이 높다.

확인해야 할 유틸리티
- SQL 내부의 자원 소모량과 실행 시간을 상세히 분석하는 TKPROK와, 해당 SQL이 인덱스를 타지 못하고 전체 테이블 스캔을 하는지 내부 길찾기 경로를 확인하는 EXPLAIN PLAN을 활용해야 한다.

















