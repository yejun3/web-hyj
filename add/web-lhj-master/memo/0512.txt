SQL 활용 능력평가
시간 : 10시 30분 ~ 12시 30분
범위:
DDL (데이터베이스 객체에 대한 조작)
CREATE, DROP, ALTER

데이트베이스 객체
- database
- 테이블
- INDEX
- VIEW


DML(데이터에 대한 조작)
SELECT,INSERT,UPDATE,DELETE

집계함수( SUM, COUNT ,MIN, AVG ,MAX)
- GROUP BY(그룹별로 집계), 
- HAIVNG : 그룹함수에 대해 조건 걸 때 사용

INDEX
- 개념 : select를 빠르게 하기 위해서 
- 어디서 사용하면 좋은가 : where, order by
- 데이터가 너무 적으면 의미가 없다
- 어떻게 만드는가 : create index ...
- 어떻게 삭제하는가

SUBQUERY
- 개념 : 쿼리문 안에 쿼리문
- select 절, from절

JOIN
- 개념 : 관계를 맺고있는 두 테이블에서 데이터를 조회하려고 할 때 
- inner join, outer join, cross join

VIEW
- 개념 : 복잡한 쿼리문의 결과를 저장하는 가상의 테이블
-----------------------------------------------------------------------
CASE문
- 조건에 따라 다른 값을 반환하는 문법

CASE
	WHEN 조건1 THEN 결과1
	WHEN 조건2 THEN 결과2
	WHEN 조건3 THEN 결과3
	ELSE 기본결과
END

CASE 컬럼명
	WHEN 값1 THEN 결과1
	WHEN 값2 THEN 결과2
	WHEN 값3 THEN 결과3
	ELSE 기본결과
END

- CASE문은 SELECT 절에서 많이 사용된다.
- order by절에서도 사용할 수 있다. -> 비즈니스 로직 기준 정렬(업무 우선순위, 처리 순서, 긴급도)
- group by에서도 사용할 수 있다.
- update 쪽에서도 조건에 맞게 데이터를 수정할 수 있는 장점

반복문
- 같은 작업을 여러번 반복 실행할 때 
- 프로시저 안에서만 작성 가능

WHILE 
begin
  변수 선언
  while 조건 do
      반복실행할 문장
      set 으로 변수값 변경
  end while
end


REPEAT : do -while과 사용법이 같다.
repeat
    반복실행할 명령;

until 조건; -> 조건이 참이 되면 종료
end repeat

LOOP : 무한루프 기반, 종료조건을 명시해야 한다.
- 라벨을 달아야 한다.

라벨:loop
    반복 실행할 문장;
    if 조건 then
        leave 라벨;
    end if;
end loop;

ITERATE -> contiue
- 현재 반복을 중단하고 다음 반복으로 바로 이동하는 명령어

라벨 : loop
	if 조건 then
		iterate 라벨;
	end if;
end loop;

MySQL의 예외처리
Handler
- Procedure 안에서 에러, 경고, 조회 결과 없음 같은 상황을 처리하는 예외처리 문법
- 프로시저 실행중 문제가 생겼을 때 프로시저가 바로 중단되지 않도록 처리 방법을 정해두는 문법

에러 처리 : 중복된 PK를 INSERT한 경우
조회 결과 없음 처리 : SELECT ... INTO의 결과가 없는 경우
커서 종료 처리 : 더 이상 읽을 데이터가 없는 경우
ㄴCURSOR : SELECT 결과를 한 줄씩 꺼내서 처리하는 기능
경고 처리 : 데이터 잘림, 형 변환 경고 등

기본 문법
DECLARE 핸들러타입 HANDLER FOR 예외
BEGIN
예외처리
END;

핸들러 타입 종류
CONTINUE : 에러 처리 후 다음 문장 계속 실행
EXIT : 에러 처리 후 BEGIN - END 블록 종료

예외종류
SQLEXCEPTION : 대부분의 SQL 에러
SQLWARNING : SQL 경고
NOT FOUND : 조회 결과 없음
MySQL 에러코드 : 1062 (중복 키 에러)

























