5월 1일, 5월 5일 휴강
내부평가 : 50% (능력평가별 출석률이 높아야 응시자격이 생김)
외부평가 : 50% 필수 능력단위 범위에서 출제

MySQL 기반
스키마 = 데이터베이스
데이터를 어떻게 저장할것인지에 대한 "설계도"다.

스키마의 구성요소
- 테이블(table)
- 뷰(view)
- 인덱스(index)
- 트리거(trigger)
- 저장 프로시저(Procedure)

스키마를 사용하는 이유
1. 데이터 구조를 정리하기 위해
- 구조를 정해서 일관성을 유지

2. 데이터 관리 단위로 사용하기 위해
- 프로젝트 별로 DB를 나눌 수 있다.
- 예 : 쇼핑몰 / 게시판 / 회원관리

3. 접근 권한 관리
- 특정 스키마만 접근 하도록 설정 가능

데이터베이스는 좀 더 깊은 개념으로 3가지로 나눈다.
1. 외부 스키마
- 사용자 입장에서 보는 구조
- EX) 화면에 보이는 데이터

2. 개념 스키마
- 전체 데이터 구조
- DB 설계자 관점

3. 내부 스키마
- 실제 저장 방식
- 디스크에 어떻게 저장되는지

Data : 가공되지 않은 단순한 사실이나 값
DB : 데이터를 체계적으로 저장해놓은 공간
DBMS : 데이터베이스를 관리하는 프로그램(소프트웨어)

DBMS의 종류
1. 계층형 데이터베이스
- 트리 구조로 데이터를 저장하는 방식
- 1: N 관계만 가능하다.
- 구조가 단순하다
- 변경이 어렵다

2. 네트워크형 데이터베이스
- 데이터를 그래프 형식으로 연결하는 방식
- N : N 관계가 가능하다
- 계층형보다는 유연하다
- 구조가 복잡하다
- 여러 개의 연결을 허용하는 DB

3. 관계형 데이터베이스
- 테이블 형태로 데이터를 저장하는 방식
- 가장 많이 사용됨
- SQL을 사용
- 데이터의 무결성을 보장하다.

4. 객체지향 데이터베이스
- 객체(Object) 형태로 데이터를 저장
- Java, C++ 객체 그대로 저장 가능하다
- 복잡한 데이터 처리에 유리
- 사용 빈도는 낮다.

5. NoSQL(Not only SQL)
- 관계형이 아닌 다양한 방식의 DB

key - value

Document
- JSON

Column
- 컬럼 중심 저장

Graph
- 관계 중심

- 대용량 처리에 강함
- 속도가 빠름
- 유연한 구조

SQL(Structured Query Language)
- DBMS 상에서 데이터를 읽고 쓰고 삭제하는 등 데이터를 관리하기 위한 일종의 프로그래밍 언어이다.

DDL(Data Definition Language) : 데이터 정의어
- 데이터베이스 객체를 관리하는 언어
- 테이블, 속성, 제약조건,뷰
- CREATE : 데이터베이스 객체 생성에 사용
- DROP : 삭제에 사용
- ALTER : 수정시 사용
- TRUNCATE : 테이블은 유지하고 데이터를 통째로 삭제

데이터베이스 생성
CREATE DATABASE IF NOT EXISTS 데이터베이스 이름
CHARCTER SET = utf8mb4
COLLATE = utf8mb4_unicode_ci

CHARCTER SET
이 데이터베이스에서 문자를 저장할 기본 문자셋(Encoding)을 지정
utf8mb4가 MySQL에서 가장 안전한 유니코드 문자셋이다.

COLLATE
정렬과 비교의 기준을 설정

테이블
- 행과 열로 이루어진 데이터의 집합
- 테이블끼리 관계를 맺고, 제약조건이 추가돼서 릴레이션라고도 부른다.

행(row)
- 가로로 묶은 데이터 셋
- 튜플, 레코드라고도 부른다.
- 행의 개수를 '카디널리티'라고 부른다.

열(column)
- 세로로 묶은 데이터 셋
- 그 테이블의 속성(attribute)을 의미한다.

자료형
문자형
CHAR(길이) : 고정형
VARCHAR(길이) : 가변형 길이
TEXT : 대용량 문자열
숫자형
INT
FLOAT
DOUBLE
날짜형
DATE
DATETIME
TIMESTAMP

제약조건
- 문제가 되는 데이터를 테이블에 추가할 수 없도록 제한하는 조건
- 테이블을 만들면서 설정할수 있고, 추후에 생성하거나 변경할수도 있다.

PRIMARY KEY(기본키)
- 데이터에 값이 있어야 하고, 겹쳐서는 안된다.

NOT NULL
- 해당 속성에는 반드시 값이 들어가야 한다.

UNIQUE(유일키)
- 속성에 들어가는 값은 유일해야 한다.

FOREIGN KEY(외래키)
- 다른 테이블의 기본키 값을 참조하는 제약조건

CHECK
- 속성에 들어갈 수 있는 데이터의 조건을 설정하는 제약조건

DEFAULT
- 값을 입력하지 않았을 때 자동으로 들어가는 기본값을 설정

CREATE TABLE 테이블명(
	속성명 자료형,
	속성명 자료형,
	속성명 자료형
);

데이터베이스 객체의 수정
ALTER

컬럼의 추가
ALTER TABLE 테이블명 ADD COLUMN 컬럼명 자료형;

컬럼의 삭제
ALTER TABLE 테이블명 DROP COLUMN 컬럼명;

컬럼명 변경
ALTER TABLE 테이블명 RENAME COLUMN 컬럼명 TO 변경할 이름;

자료형의 변경
ALTER TABLE 테이블명 MODIFY COLUMN 컬럼명 자료형;

제약조건 추가
ALTER TABLE 테이블명 ADD 제약조건 종류(컬럼);

제약조건 삭제
ALTER TABLE 테이블명 DROP 제약조건 종류(컬럼);
- 기본키의 경우는 이름없이 삭제가 가능
- 나머지 제약조건은 이름을 명시해줘야 한다.

- NOT NULL과 DEFAULT는 MODIFY를 사용한다.
ALTER TABLE 테이블명 MODIFY 컬럼명 데이터타입;

무결성
- 데이터베이스에 저장된 데이터가 현실 세계의 실제값과 일치하는 정확성을 의미한다.

1. 개체 무결성
- PK로 설정된 컬럼은 반드시 고유한 값을 가져야 한다. 그리고 NULL이 될 수 없다.

2. 참조 무결성
- 외래키(FK)값은 NULL이거나, 반드시 참조하는 테이블의 PK에 들어있는 값과 일치해야 한다.
- 존재하지 않는 값은 외래키로 저장할 수 없다.

3. 도메인 무결성
- 특정 속성의 값이 미리 정의된 유효한 값 범위 안에 있어야 한다.

4. 사용자 정의 무결성
- 사용자가 논리적으로 필요한 규칙을 직접 정의하는 것
- 나이는 음수가 들어올 수 없다. 가입일은 오늘 이후일수 없다.

테이블을 만드는 기술적인 방법

테이블을 만들기 위한 기획 (모델링)

식별 -> 값이 겹치면 안됨
PK -> 값이 없으면 안되고, 겹치면 안됨
FK -> PK에 들어있는 값을 참고

요구사항
회원은 도서를 대출할 수 있다.
도서는 도서번호 제목과 출판사를 가진다.
회원은 회원번호 이름과 전화번호를 가진다.
한 회원은 여러 권의 도서를 대출할 수 있다.
하나의 도서는 여러번 대여될 수 있다.
대여 시 대여일과 반납일이 기록된다.

1단계
엔티티(Entity) 도출하기

2단계
속성(Attribute) 정의하기

3단계
관계(Relationship) 정의하기

4단계
Chen 표기법으로 ERD 그리기

요구사항
환자는 병원에 예약을 할 수 있다.
의사는 환자를 진료할 수 있다.
환자는 환자 번호, 이름, 주민번호를 가진다.
의사는 의사 번호, 이름, 전문분야를 가진다.
예약에는 예약 번호, 예약일과 상태가 포함된다.
한 환자는 여러 예약을 할 수 있다.
한 의사는 여러 환자의 예약을 담당할 수 있다.

요구사항
회원은 상품을 주문할 수 있다.
회원은 회원번호, 이름, 주소를 가진다.
상품은 상품번호, 이름, 가격을 가진다.
주문은 여러 상품을 포함할 수 있다.
상품은 여러 주문에 포함할 수 있다.
주문에는 주문번호, 주문일, 상태가 포함된다.
주문된 상품에는 수량이 기록된다.

까마귀발 표기법
 |  = 하나

 < =  여러 개

 O = 선택

회원은 게시글을 작성할 수 있다.
게시글에는 여러 개의 댓글이 달릴 수 있다.
회원은 댓글을 작성할 수 있다.
회원은 게시글을 하나도 작성하지 않을 수 있다.
댓글은 반드시 하나의 게시글에 속한다.


정규화
ERD 내에서 중복 요소를 찾아 제거해나가는 과정

1차 ~ 5차 정규화가 존재한다.

1정규형
- 한칸에는 하나의 값만 들어가야 한다.

2정규형
- 1정규형을 만족하면서 기본키의 일부에만 의존하는 속성을 분리한다.

복합키
- 여러개의 속성을 묶어서 하나의 기본키로 사용하는 것이다.
- 여러 컬럼의 값을 조합해서 겹치는 값이 없으면 된다.

학생이름, 수강과목, 나이

학생이름, 나이

학생이름, 수강과목

3차 정규화
- 2차 정규화를 만족하는 상태에서 기본키가 아닌 다른 컬럼이 다른 일반컬럼을 결정하면 분리한다.

(PK)
학생 번호, 학생명,   학과번호, 학과명, 학과사무실
1		홍길동

학생 번호 -> 학과 번호
학과 번호 -> 학과명, 학과 사무실

학생
학생 번호, 학생명, 학과 번호

학과
학과 번호, 학과명, 학과 사무실

이행함수 종속
A -> B, B -> C, A -> C가 성립되는 관계
중간 속성을 통해 간접적으로 결정되는 관계

정규화를 통해서 테이블을 너무 많이 쪼개게되면 조회를 할 때 성능이 떨어지게 된다.

역정규화를 하기도 한다.

테이블을 만들기 전에 모델링을 잘 하는것이 매우 중요하다.

꽃 테이블와 화분 테이블 2개가 필요하고 꽃을 구매할 때 화분도 같이 구매한다.
꽃은 꽃 이름, 색깔, 가격이 존재한다.
화분 제품번호, 화분 색깔, 모양, 꽃 이름이 존재한다.

꽃
이름(PK)
-----------
색깔
가격

화분
제품번호(PK)
------------
화분 색깔
모양
꽃 이름

사용자는 게시글을 작성할 수 있다.
게시글에는 여러 개의 댓글이 달릴 수 있다.
댓글은 반드시 하나의 게시글에 속한다.
사용자는 댓글도 작성할 수 있다.
유저에는 유저번호, 이름만 있다.
게시글에는 게시글 번호, 유저 번호, 제목, 내용이 있다.
댓글에는 댓글 번호, 게시글 번호, 유저번호, 내용, 생성 시간이 있다.











