상속
ㄴ 관련있는것들끼리 묶는것
공통적인 속성(필드, 멤버)이나 동작(메서드)을 부모쪽에 몰아놓는것
자식은 부모클래스를 상속받아서 사용
is-a 관계
강아지는 동물이다.(dog is a animal)
N차 상속이 가능함 상속 -> 상속 -> 상속
꼭 부모가 아닌 조상의 필드나 메서드를 사용할 수 있다.
Car -> Bus -> SchoolBus
다중상속이 불가능하다
Object : 모든 클래스의 부모클래스

Arrays.toString(배열)

sealed : 봉인된 클래스
permit : 내가 허용한 클래스만 상속을 할 수 있게 한다.
영어공부도 틈틈히 해야함!! (중요)
읽기,말하기,듣기,쓰기

인터페이스
일반 필드 X
상수, 추상메서드, 디폴트메서드, 정적메서드, private 메서드
ㄴ 동작에 대한 가이드라인 제공
같은 범주에 있는 것들이 아니어도됨
fly();
class Bird{};
class Airplane{};

추상클래스와 마찬가지로 직접 객체를 만드는것이 불가능하다.
인터페이스를 구현할 클래스가 있어야 한다.

class Impl implements 인터페이스명{};

추상메서드를 반드시 오버라이딩해야 한다.

다중구현이 가능하다.
class Impl implements interface1,interface2{};
- 각 인터페이스에 있던 추상메서드를 전부 오버라이딩 해야한다.

다중 상속이 가능하다.
- 상속은 인터페이스 끼리만 가능하다.
interface interA extends InterB, InterC {};

인터페이스의 다형성
구현클래스의 객체를 인터페이스 타입의 변수에 넣는것은 가능하다.

중첩클래스
- 많이 사용되지는 않지만 특정한 상황에서 쓰인다.
언제쓰이나?
1. 클래스 내부에서만 쓰는 전용 객체
(외부에 공개할 필요가 없음)

정적내부 클래스
내부클래스인데 static이 붙은 형태이다.
바깥 객체에 소속된 내부 클래스처럼 보이지만, 실제로는 바깥객체 없이도 독립적으로 만들수 있는 내부 클래스

RemoteControl 클래스 만들기
필드
- String power "Off"로 초기화하기
Button 인스턴스 내부 클래스
메서드 press()
ㄴ 외부 클래스의 power 값을 "ON"으로 바꾸고 
"전원을 켭니다"를 출력하기
Main에서 RemoteControl 객체를 생성한 뒤, Button객체를 생성하여 press()메서드 호출하기
마지막에 power값도 출력하기

예시
전원을 켭니다
현재 전원 상태 : On

정적내부클래스로 계산 결과 객체 만들기

Calculator클래스
메서드
add(int a, int b) : 두 수를 더한 뒤 Result객체로 결과를 넣어서 반환하세요
Result 정적 내부 클래스
필드
- value
생성자를 만들어 계산 결과를 저장하세요
메서드
show() : "계산결과 : X"형식으로 출력하세요

main에서 add()를 호출하고 반환된 Result객체의 show()를 호출하세요

Calculator 인터페이스
- void add(int x, int y);

Main에서 익명객체를 생성하여 두 수의합을 출력하세요

Java의 모듈 시스템을 이용하여 회원 등록 프로그램 만들기
member.module
member 패키지
Member클래스
필드(private)
- 이름
- 나이
생성자를 통하여 이름과 나이를 초기화 하세요

service.module
service패키지
MemberService클래스
메서드
void register(Member member);
"회원 등록을 시작합니다."
"회원 이름 : xxx"
"회원 나이 : xx"
"회원 등록이 완료되었습니다."

app.module
app 패키지
Main클래스
키보드에서 사용자의 이름과 나이를 입력받으세요
입력받은 이름과 나이를 사용하여 Member객체를 생성하세요
MemberService객체를 만들어 register()를 이용해 회원을 등록하세요

모듈 의존 관계
app.module -> service.module -> member.module





















