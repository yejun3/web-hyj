package ex_2_method;

public class TimeTableMain {
	public static void main(String[] args) {
		TimeTable a = new TimeTable();
		a.showTable(5,5);
		}
  
  //1. 클래스의 객체를 생성한다.  TimeTable a = new TimeTable();
  //2. 객체를 통해 매서드를 호출한다.  a.showTable(a,b);
  //2-1 return을 통해 빈환값을 받는 경우 연산결과를 변수에 대입한다.
  //2-2 반환값이 없는 경우 호출만 한다.
	
}
